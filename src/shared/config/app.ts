/**
 * App-level constants. This deployment serves a single wishlist, whose slug
 * matches the seeded row — used for the root redirect and the brand/home link.
 */
export const DEFAULT_WISHLIST_SLUG = 'demo';
