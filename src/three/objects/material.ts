/**
 * Shared metallic look from orb-stage.js: high metalness + strong environment
 * reflections turn a blue-ish tint into polished, tinted chrome/silver.
 */
export const METAL = {
  metalness: 0.92,
  envMapIntensity: 2.6,
} as const;
